[project]
name = "nail_unet"
version = "0.1.0"
description = "Fingernail segmentation and sorting utilities"
requires-python = ">=3.10"

[tool.black]
line-length = 88

target-version = ["py310"]

[tool.isort]
profile = "black"
